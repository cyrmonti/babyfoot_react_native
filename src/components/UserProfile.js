import React from 'react';
import { View, Text } from 'react-native';

const UserProfile = () => {
    return (
        <View>
            <Text>User profile info here.</Text>
        </View>

    );
};

export default UserProfile;